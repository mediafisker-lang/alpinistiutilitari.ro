import type {
  AssociationProgress,
  CommunityLink,
  UpdateItem,
} from "@/types/database";

export const fallbackProgress: AssociationProgress[] = [];

export const fallbackCommunityLinks: CommunityLink[] = [];

export const fallbackUpdates: UpdateItem[] = [];
